# VPS deployment is handled by deploy-vps.yml.
# This file is intentionally minimal — it exists only to satisfy any external
# branch-protection rules that reference a "Deploy" workflow.
name: Deploy

on:
  workflow_dispatch:

jobs:
  noop:
    name: No-op (see deploy-vps.yml)
    runs-on: ubuntu-latest
    steps:
      - run: echo "Deploy to VPS is handled by the 'Deploy to mitch-cloud' workflow."
